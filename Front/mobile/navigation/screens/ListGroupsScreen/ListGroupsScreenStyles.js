import { StyleSheet } from "react-native";

export const listGroupsScreenStyles = StyleSheet.create({
	containerBig: {
		width: "100%",
		flexDirection: "column",
		backgroundColor: "white",
		marginTop: 55,
	},
});
